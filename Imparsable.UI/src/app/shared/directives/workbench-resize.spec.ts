import { WorkbenchResize } from './workbench-resize';

describe('WorkbenchResize', () => {
  it('should create an instance', () => {
    const directive = new WorkbenchResize();
    expect(directive).toBeTruthy();
  });
});
